[project]
name = "ai-employee"
version = "0.1.0"
description = "Personal AI Employee - Bronze Tier"
requires-python = ">=3.13"
dependencies = [
    "watchdog>=4.0.0",
]

[project.scripts]
file-watcher = "watchers.filesystem_watcher:main"
